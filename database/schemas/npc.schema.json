{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "NPC",
  "description": "A person standing somewhere who knows something. Distinct from a character entity, which is biography: canon already holds 41 characters, and this is what makes one of them met rather than merely described.\n\nSettlements are information ecosystems, not quest boards. An NPC gives a lead, a word, an old map or a cup of tea -- never a task list.",
  "type": "object",
  "required": [
    "id",
    "type",
    "name"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^npc_[a-z0-9_]+$"
    },
    "type": {
      "const": "npc"
    },
    "name": {
      "type": "string"
    },
    "character": {
      "type": "string",
      "description": "Optional character entity id, when this is someone canon already knows. Leave empty for a fisherman who exists only to say one useful thing."
    },
    "found_at": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "point_of_interest ids where they can be met."
    },
    "role": {
      "type": "string",
      "description": "How they read on meeting -- fisherman, scholar, herder, child. Not a class."
    },
    "language": {
      "type": "string",
      "description": "vocabulary language id they speak. If Varuna lacks the words, what they say arrives incomplete."
    },
    "knows": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "discovery or field_question ids they can move along. What they offer is a lead, not an answer."
    },
    "lines": {
      "type": "array",
      "description": "What they say, and what it takes to hear it.",
      "items": {
        "type": "object",
        "required": [
          "text"
        ],
        "properties": {
          "text": {
            "type": "string"
          },
          "requires": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "discovery or vocabulary ids needed before this is said, or understood."
          },
          "gives": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "discovery, vocabulary, field_question or recipe ids this opens. A `recipe_` id here is somebody showing you how, which is the only way a recipe carrying `taught_by` is ever got."
          },
          "costs": {
            "type": "string",
            "pattern": "^item_[a-z0-9_]+$",
            "description": "An item handed over to hear this. The gift loop, built out of the line machinery that was already there rather than a `wants` structure beside it -- a person who needs a rope and tells you something once they have one is exactly a line with a price.\n\n**Not a currency and not a trade.** There is no price table in canon and there should not be; this is one named object one person wants, and what they say after is the whole return. A line with a cost is spent when its gifts are held, on the same rule as every other line."
          }
        },
        "additionalProperties": true
      }
    },
    "epochs": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "notes": {
      "type": "string"
    },
    "canon": {
      "type": "string",
      "enum": [
        "primary",
        "secondary",
        "draft"
      ],
      "default": "primary"
    },
    "sources": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "would_settle": {
      "type": "boolean",
      "default": false,
      "description": "Whether this person would join the settlement at the end, if you helped them. Not everyone would: some are rooted where they are, and some would rather not be found. The endgame roster is the people who would, crossed with the discoveries that helped them -- nothing separate is tracked."
    },
    "descended_from": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Character ids this person comes from, when the line matters and the person is not that character. Bekh is Mask Family by descent from Kavik across enough generations that the name means little to her -- putting `character_kavik` in `character` would have said she IS him. Found while authoring the Lothal slice, which is what the slice was for."
    }
  },
  "additionalProperties": false
}
